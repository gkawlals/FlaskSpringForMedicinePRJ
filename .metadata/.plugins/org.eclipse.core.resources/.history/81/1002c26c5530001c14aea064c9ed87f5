<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="poly.persistance.mapper.IOcrMapper">

	<!-- 회원 가입하기(회원정보 등록하기) -->
	<insert id="InsertImage" parameterType="OcrDTO">
		INSERT INTO Medicine.OCR_INFO
			(
				SAVE_FILE_NAME,
				SAVE_FILE_PATH,
				ORG_FILE_NAME,
				SAVE_FOLDER_NAME,
				REG_ID,
				CHG_ID
			)
		VALUES(
				#{save_file_name},
				#{save_file_path},
				#{org_file_name},
				#{user_id},
				#{user_id}
		)
	</insert>
	
	<insert id="InsertMD" parameterType="OcrDTO">
		INSERT INTO Medicine.OCR_INFO
			(
				MEDICINE_NAME,
				RECEIVE_DT
			)
		VALUES(
				#{medicine_name},
				#{receive_dt}
				
		)
	</insert>
	
	
		
</mapper>
